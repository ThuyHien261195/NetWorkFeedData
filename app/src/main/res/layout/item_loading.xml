<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:gravity="center"
    android:orientation="horizontal"
    android:paddingBottom="@dimen/item_loading_vertical_spacing"
    android:paddingTop="@dimen/item_loading_vertical_spacing">

    <ProgressBar
        android:id="@+id/progress_bar_load_more"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginRight="@dimen/progress_bar_horizontal_spacing"
        android:indeterminate="true"
        android:indeterminateTint="@color/color_loading_progress_bar"
        android:indeterminateTintMode="src_atop" />

    <TextView
        android:id="@+id/text_loading"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="@string/title_loading"
        android:textColor="@android:color/black"
        android:textSize="@dimen/title_loading_text_size" />
</LinearLayout>